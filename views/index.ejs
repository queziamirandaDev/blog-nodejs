<!DOCTYPE html>
<html lang="pt-br">
     
    <%- include('./paciais/head.ejs') %>
<body>
        <!-- Inclusão do parcial NAV   -->

        <%- include('./paciais/nav.ejs') %>

    <div class="conteudo-blog">
        <h2>Todo Blog</h2>

        <% if (blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>

            <h3 class="titulo"><%= blog.titulo %></h3>
            <p class="conteudo"><%= blog.conteudo %></p>
       <% }) %>
       <% } else { %>
        
        <p>A lista do blog esta vazia</p>
    
       <% } %>
    </div>

     <%- include('./paciais/footer.ejs') %>

</body>
</html>